<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="@dimen/small_space">

    <TextView
        android:id="@+id/date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="12:00PM"
        android:textColor="@color/white"
        android:textSize="@dimen/fixed_size_for_hours_date"
        app:layout_constraintBottom_toTopOf="@+id/hour_icon"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/hour_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_few_clouds"
        app:layout_constraintBottom_toTopOf="@+id/hour_temp"
        app:layout_constraintEnd_toEndOf="@+id/date"
        app:layout_constraintStart_toStartOf="@+id/date"
        app:layout_constraintTop_toBottomOf="@+id/date" />

    <TextView
        android:id="@+id/hour_temp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="23°"
        android:textSize="@dimen/fixed_size_for_hours_temperature_days_temperature"
        android:textColor="@color/white"
        app:layout_constraintBottom_toTopOf="@+id/hour_icon"
        app:layout_constraintEnd_toEndOf="@+id/hour_icon"
        app:layout_constraintStart_toStartOf="@+id/hour_icon"
        app:layout_constraintTop_toBottomOf="@+id/hour_icon" />
</androidx.constraintlayout.widget.ConstraintLayout>